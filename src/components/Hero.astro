---
import heroImage1 from '../assets/images/1.png?url';
import heroImage2 from '../assets/images/2.png?url';
import heroImage3 from '../assets/images/3.png?url';
---

<section class="relative h-screen overflow-hidden">
  <div class="relative w-full h-full">
    <!-- Slide 1 -->
    <div class="slide active absolute top-0 left-0 w-full h-full opacity-0 transition-opacity duration-700 ease-in-out flex items-center z-10">
      <div class="absolute top-0 left-0 w-full h-full bg-cover bg-center bg-no-repeat" style={`background-image: url(${heroImage1})`}>
        <div class="absolute top-0 left-0 w-full h-full bg-gradient-to-r from-black/80 via-black/60 to-black/20"></div>
      </div>
      <div class="relative z-10 w-full">
        <div class="max-w-7xl mx-auto px-5">
          <h2 class="relative text-white text-sm font-medium uppercase tracking-[0.2em] mb-6 opacity-90">
            <span class="relative z-10 bg-gradient-to-r from-white via-red-200 to-white bg-clip-text text-transparent font-semibold">MÁS DE 10 AÑOS EN EL MERCADO</span>
            <div class="absolute -left-2 -right-2 top-1/2 h-px bg-gradient-to-r from-transparent via-white/30 to-transparent transform -translate-y-1/2"></div>
          </h2>
          <h1 class="text-white text-5xl lg:text-6xl font-bold leading-tight mb-6 max-w-4xl">Bienvenidos a HARO</h1>
          <p class="text-white text-lg leading-relaxed mb-8 max-w-2xl opacity-95">Expertos en repuestos de cremalleras hidráulicas. Una década de trayectoria ofreciendo calidad y confianza al mercado peruano.</p>
          <a href="https://wa.me/51992767584" target="_blank" class="inline-block bg-red-600 text-white px-8 py-4 rounded-md text-base font-semibold uppercase tracking-wide hover:bg-red-700 hover:-translate-y-0.5 hover:shadow-lg hover:shadow-red-600/40 transition-all duration-300">Contáctanos</a>
        </div>
      </div>
    </div>

    <!-- Slide 2 -->
    <div class="slide absolute top-0 left-0 w-full h-full opacity-0 transition-opacity duration-700 ease-in-out flex items-center z-10">
      <div class="absolute top-0 left-0 w-full h-full bg-cover bg-center bg-no-repeat" style={`background-image: url(${heroImage2})`}>
        <div class="absolute top-0 left-0 w-full h-full bg-gradient-to-l from-black/80 via-black/60 to-black/20"></div>
      </div>
      <div class="relative z-10 w-full flex justify-end">
        <div class="max-w-7xl mx-auto px-5 w-full flex justify-end">
          <div class="text-right max-w-2xl">
            <h2 class="relative text-white text-sm font-medium uppercase tracking-[0.2em] mb-6 opacity-90">
            <span class="relative z-10 bg-gradient-to-r from-white via-red-200 to-white bg-clip-text text-transparent font-semibold">MÁS DE 10 AÑOS EN EL MERCADO</span>
            <div class="absolute -left-2 -right-2 top-1/2 h-px bg-gradient-to-r from-transparent via-white/30 to-transparent transform -translate-y-1/2"></div>
          </h2>
            <h1 class="text-white text-5xl lg:text-6xl font-bold leading-tight mb-6">Servos Hidráulicos</h1>
            <p class="text-white text-lg leading-relaxed mb-8 opacity-95">Especialistas en repuestos de servos hidráulicos con más de una década de experiencia. Calidad garantizada para el mercado peruano.</p>
            <a href="https://wa.me/51992767584" target="_blank" class="inline-block bg-red-600 text-white px-8 py-4 rounded-md text-base font-semibold uppercase tracking-wide hover:bg-red-700 hover:-translate-y-0.5 hover:shadow-lg hover:shadow-red-600/40 transition-all duration-300">Contáctanos</a>
          </div>
        </div>
      </div>
    </div>

    <!-- Slide 3 -->
    <div class="slide absolute top-0 left-0 w-full h-full opacity-0 transition-opacity duration-700 ease-in-out flex items-center z-10">
      <div class="absolute top-0 left-0 w-full h-full bg-cover bg-center bg-no-repeat" style={`background-image: url(${heroImage3})`}>
        <div class="absolute top-0 left-0 w-full h-full bg-gradient-to-r from-black/80 via-black/60 to-black/20"></div>
      </div>
      <div class="relative z-10 w-full">
        <div class="max-w-7xl mx-auto px-5">
          <h2 class="relative text-white text-sm font-medium uppercase tracking-[0.2em] mb-6 opacity-90">
            <span class="relative z-10 bg-gradient-to-r from-white via-red-200 to-white bg-clip-text text-transparent font-semibold">MÁS DE 10 AÑOS EN EL MERCADO</span>
            <div class="absolute -left-2 -right-2 top-1/2 h-px bg-gradient-to-r from-transparent via-white/30 to-transparent transform -translate-y-1/2"></div>
          </h2>
          <h1 class="text-white text-5xl lg:text-6xl font-bold leading-tight mb-6 max-w-4xl">ATENCIÓN PERSONALIZADA</h1>
          <p class="text-white text-lg leading-relaxed mb-8 max-w-2xl opacity-95">En HARO, cada cliente recibe asesoría especializada y soluciones a medida en repuestos de dirección hidráulica. Calidad y confianza garantizadas.</p>
          <a href="https://wa.me/51992767584" target="_blank" class="inline-block bg-red-600 text-white px-8 py-4 rounded-md text-base font-semibold uppercase tracking-wide hover:bg-red-700 hover:-translate-y-0.5 hover:shadow-lg hover:shadow-red-600/40 transition-all duration-300">Contáctanos</a>
        </div>
      </div>
    </div>

  <!-- Navigation dots -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 flex gap-4 z-30">
    <span class="w-3 h-3 rounded-full bg-white/50 cursor-pointer transition-all duration-300 hover:bg-white/80 nav-dot active" onclick="currentSlide(1)"></span>
    <span class="w-3 h-3 rounded-full bg-white/50 cursor-pointer transition-all duration-300 hover:bg-white/80 nav-dot" onclick="currentSlide(2)"></span>
    <span class="w-3 h-3 rounded-full bg-white/50 cursor-pointer transition-all duration-300 hover:bg-white/80 nav-dot" onclick="currentSlide(3)"></span>
  </div>
</section>

<script>
document.addEventListener('DOMContentLoaded', function() {
  let slideIndex = 0;
  const slides = document.querySelectorAll('.slide');
  const dots = document.querySelectorAll('.nav-dot');

  function showSlide(n) {
    // Remove active class from all slides and dots
    slides.forEach((slide, index) => {
      slide.classList.remove('active');
      slide.style.zIndex = '1'; // Reset z-index
    });
    dots.forEach(dot => dot.classList.remove('active'));
    
    slideIndex = (n + slides.length) % slides.length;
    
    // Add active class to current slide and dot
    slides[slideIndex].classList.add('active');
    slides[slideIndex].style.zIndex = '10'; // Bring active slide to front
    dots[slideIndex].classList.add('active');
  }

  function currentSlide(n) {
    showSlide(n - 1);
  }

  // Initialize first slide
  showSlide(0);

  // Auto slide
  setInterval(() => {
    showSlide(slideIndex + 1);
  }, 5000);

  // Make currentSlide globally available
  window.currentSlide = currentSlide;
});
</script>

<style>
  .slide.active {
    opacity: 1 !important;
  }

  .nav-dot.active {
    background: rgb(220 38 38) !important;
    transform: scale(1.2) !important;
  }

  .relative.h-screen {
    height: calc(100vh - 80px) !important;
    margin-top: 108px !important;
  }

  @media (max-width: 768px) {
    .relative.h-screen {
      height: calc(100vh - 109px) !important;
      margin-top: 0 !important;
    }

    .text-5xl.lg\:text-6xl {
      font-size: 28px !important;
    }

    .text-lg {
      font-size: 16px !important;
    }

    .px-8.py-4 {
      padding: 12px 24px !important;
      font-size: 14px !important;
    }
  }
</style>