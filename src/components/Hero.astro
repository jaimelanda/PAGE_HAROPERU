---
import heroImage1 from '../assets/images/1.png?url';
import heroImage2 from '../assets/images/2.png?url';
import heroImage3 from '../assets/images/3.png?url';
---

<section class="hero">
  <div class="hero-slider">
    <!-- Slide 1 -->
    <div class="slide active">
      <div class="slide-background" style={`background-image: url(${heroImage1})`}></div>
      <div class="slide-content">
        <div class="container">
          <h2 class="slide-subtitle">MÁS DE 10 AÑOS EN EL MERCADO</h2>
          <h1 class="slide-title">Bienvenidos a HARO<br>Dirección Hidráulica</h1>
          <p class="slide-description">Expertos en repuestos de cremalleras hidráulicas. Una década de trayectoria ofreciendo calidad y confianza al mercado peruano.</p>
          <a href="https://wa.me/51992767584" target="_blank" class="btn-contact">Contáctanos</a>
        </div>
      </div>
    </div>

    <!-- Slide 2 -->
    <div class="slide">
      <div class="slide-background" style={`background-image: url(${heroImage2})`}></div>
      <div class="slide-content">
        <div class="container">
          <h2 class="slide-subtitle">MÁS DE 10 AÑOS EN EL MERCADO</h2>
          <h1 class="slide-title">Servos Hidráulicos<br>de Calidad</h1>
          <p class="slide-description">Especialistas en repuestos de servos hidráulicos con más de una década de experiencia. Calidad garantizada para el mercado peruano.</p>
          <a href="https://wa.me/51992767584" target="_blank" class="btn-contact">Contáctanos</a>
        </div>
      </div>
    </div>

    <!-- Slide 3 -->
    <div class="slide">
      <div class="slide-background" style={`background-image: url(${heroImage3})`}></div>
      <div class="slide-content">
        <div class="container">
          <h2 class="slide-subtitle">MÁS DE 10 AÑOS EN EL MERCADO</h2>
          <h1 class="slide-title">ATENCIÓN PERSONALIZADA</h1>
          <p class="slide-description">En HARO, cada cliente recibe asesoría especializada y soluciones a medida en repuestos de dirección hidráulica. Calidad y confianza garantizadas.</p>
          <a href="https://wa.me/51992767584" target="_blank" class="btn-contact">Contáctanos</a>
        </div>
      </div>
    </div>

  <!-- Navigation dots -->
  <div class="slider-nav">
    <span class="nav-dot active" onclick="currentSlide(1)"></span>
    <span class="nav-dot" onclick="currentSlide(2)"></span>
    <span class="nav-dot" onclick="currentSlide(3)"></span>
  </div>
</section>

<script>
let slideIndex = 0;
const slides = document.querySelectorAll('.slide');
const dots = document.querySelectorAll('.nav-dot');

function showSlide(n) {
  slides.forEach(slide => slide.classList.remove('active'));
  dots.forEach(dot => dot.classList.remove('active'));
  
  slideIndex = (n + slides.length) % slides.length;
  
  slides[slideIndex].classList.add('active');
  dots[slideIndex].classList.add('active');
}

function currentSlide(n) {
  showSlide(n - 1);
}

// Auto slide
setInterval(() => {
  showSlide(slideIndex + 1);
}, 5000);

window.currentSlide = currentSlide;
</script>

<style>
.hero {
  position: relative;
  height: 100vh;
  overflow: hidden;
  margin-top: -108px;
  padding-top: 108px;
}

.hero-slider {
  position: relative;
  width: 100%;
  height: 100%;
}

.slide {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  opacity: 0;
  transition: opacity 0.8s ease-in-out;
  display: flex;
  align-items: center;
}

.slide.active {
  opacity: 1;
}

.slide-background {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
}

.slide-background::after {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: linear-gradient(to right, rgba(0, 0, 0, 0.8) 0%, rgba(0, 0, 0, 0.6) 40%, rgba(0, 0, 0, 0.2) 70%, transparent 100%);
}

.slide-content {
  position: relative;
  z-index: 2;
  width: 100%;
}

.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 20px;
}

.slide-subtitle {
  color: #fff;
  font-size: 16px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 1px;
  margin-bottom: 20px;
  opacity: 0.9;
}

.slide-title {
  color: #fff;
  font-size: clamp(32px, 5vw, 56px);
  font-weight: 700;
  line-height: 1.2;
  margin-bottom: 25px;
  max-width: 800px;
}

.slide-description {
  color: #fff;
  font-size: 18px;
  line-height: 1.6;
  margin-bottom: 30px;
  max-width: 600px;
  opacity: 0.95;
}

.highlight-stat {
  color: var(--primary-red);
  font-size: 48px;
  font-weight: 900;
  margin-bottom: 30px;
  text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
}

.warning-text {
  color: #ff4444;
  font-size: 20px;
  font-weight: 700;
  text-transform: uppercase;
  margin-bottom: 30px;
  text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
}

.btn-contact {
  background: var(--primary-red);
  color: #fff;
  padding: 15px 30px;
  border-radius: 5px;
  font-size: 16px;
  font-weight: 600;
  text-decoration: none;
  display: inline-block;
  transition: all 0.3s ease;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.btn-contact:hover {
  background: var(--primary-red-dark);
  transform: translateY(-2px);
  box-shadow: 0 5px 15px rgba(220, 38, 38, 0.4);
}

.slider-nav {
  position: absolute;
  bottom: 30px;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  gap: 15px;
  z-index: 3;
}

.nav-dot {
  width: 12px;
  height: 12px;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.5);
  cursor: pointer;
  transition: all 0.3s ease;
}

.nav-dot.active {
  background: var(--primary-red);
  transform: scale(1.2);
}

.nav-dot:hover {
  background: rgba(255, 255, 255, 0.8);
}

@media (max-width: 768px) {
  .hero {
    height: 70vh;
    margin-top: -65px;
    padding-top: 65px;
  }

  .slide-title {
    font-size: 28px;
  }

  .slide-description {
    font-size: 16px;
  }

  .highlight-stat {
    font-size: 36px;
  }

  .warning-text {
    font-size: 16px;
  }

  .btn-contact {
    padding: 12px 24px;
    font-size: 14px;
  }
}
</style>