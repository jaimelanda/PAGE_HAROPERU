---
import tiendaFisicaHaro from '../assets/images/tienda_fisica_haro.jpg?url';
---

<section class="about-section" id="nosotros">
  <div class="container">
    <div class="about-content">
      <div class="about-text">
        <span class="section-label">Somos</span>
        <h2>HARO PERÚ PACÍFICO S.A.C.</h2>
        <h3>Importación y distribución mayorista de repuestos de dirección hidráulica</h3>
        
        <p>
          Somos una empresa especializada en la <strong>importación y distribución</strong> 
          de repuestos para dirección hidráulica. Con más de 
          <strong>10 años de experiencia</strong> en el mercado peruano y un portafolio de más de 5,000 repuestos disponibles.
        </p>
        
        <div class="features">
          <div class="feature">
            <i class="fas fa-boxes"></i>
            <div class="feature-content">
              <h4>Disponibilidad</h4>
            </div>
          </div>
          
          <div class="feature">
            <i class="fas fa-shield-alt"></i>
            <div class="feature-content">
              <h4>confiabilidad</h4>
            </div>
          </div>
          
          <div class="feature">
            <i class="fas fa-microchip"></i>
            <div class="feature-content">
              <h4>tecnología</h4>
            </div>
          </div>
        </div>
        
      </div>
      
      <div class="about-image">
        <div class="image-container">
          <!-- TODO: Reemplazar con imagen de productos HARO -->
          <img src={tiendaFisicaHaro} alt="Almacén de repuestos automotrices HARO - Bodega organizada con miles de productos" class="warehouse-image" />
          <div class="image-stats">
            <div class="stat-badge">
              <span class="stat-number" data-target="10" data-suffix="+">0+</span>
              <span class="stat-label">Años</span>
            </div>
            <div class="stat-badge">
              <span class="stat-number" data-target="100" data-suffix="%">0%</span>
              <span class="stat-label">Confianza</span>
            </div>
          </div>
        </div>
      </div>
    </div>
    
    <!-- Misión y Visión -->
    <div class="mission-vision-section">
      <div class="mission-vision-grid">
        <div class="mission-card">
          <div class="card-icon">
            <i class="fas fa-bullseye"></i>
          </div>
          <div class="card-content">
            <h3>Nuestra Misión</h3>
            <p>
              Brindar soluciones integrales en repuestos de dirección hidráulica de la más alta calidad, 
              garantizando la seguridad y confiabilidad en cada vehículo. Nos comprometemos a ofrecer 
              productos innovadores con el respaldo de fabricantes reconocidos mundialmente, 
              acompañados de un servicio técnico especializado que supere las expectativas de nuestros clientes.
            </p>
          </div>
        </div>
        
        <div class="vision-card">
          <div class="card-icon">
            <i class="fas fa-eye"></i>
          </div>
          <div class="card-content">
            <h3>Nuestra Visión</h3>
            <p>
              Ser la empresa líder en el mercado peruano de importación y distribución de repuestos 
              para dirección hidráulica, reconocida por nuestra excelencia en calidad, innovación tecnológica 
              y compromiso con la satisfacción del cliente. Aspiramos a expandir nuestra presencia a nivel 
              nacional, siendo el socio estratégico preferido por talleres y distribuidores en toda América Latina.
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .about-section {
    padding: 80px 0;
    background: #f8f9fa;
    overflow-x: hidden;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
    width: 100%;
    box-sizing: border-box;
  }

  .about-content {
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: 40px;
    align-items: flex-start;
    width: 100%;
  }

  .section-label {
    color: #d81324;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 2px;
    font-size: 0.85rem;
    position: relative;
    display: inline-block;
    padding-left: 20px;
  }

  .section-label::before {
    content: '';
    position: absolute;
    left: 0;
    top: 50%;
    transform: translateY(-50%);
    width: 12px;
    height: 2px;
    background: linear-gradient(90deg, #d81324, #e01729);
    border-radius: 2px;
  }

  .about-text h2 {
    font-size: 2.3rem;
    color: #1a1a1a;
    margin: 20px 0 15px 0;
    font-weight: 800;
    line-height: 1.2;
    background: linear-gradient(135deg, #1a1a1a 0%, #333 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    white-space: nowrap;
  }

  .about-text h3 {
    font-size: 1.4rem;
    color: #555;
    margin-bottom: 35px;
    font-weight: 500;
    line-height: 1.4;
    font-style: italic;
    opacity: 0.9;
  }

  .about-text p {
    color: #555;
    line-height: 1.9;
    margin-bottom: 25px;
    font-size: 1.1rem;
    font-weight: 400;
    text-align: justify;
    position: relative;
  }

  .about-text p:first-of-type {
    font-size: 1.15rem;
    margin-bottom: 30px;
  }

  .about-text p strong {
    color: #d81324;
    font-weight: 700;
    position: relative;
  }

  .features {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 25px;
    margin: 35px 0;
  }

  .feature {
    background: rgba(255, 255, 255, 0.9);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(216, 19, 36, 0.1);
    border-radius: 16px;
    padding: 16px 20px;
    display: flex;
    align-items: center;
    gap: 15px;
    text-align: left;
    transition: all 0.3s ease;
    position: relative;
    overflow: visible;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
    min-height: 65px;
  }

  .feature i {
    font-size: 1.3rem;
    color: white;
    width: 48px;
    height: 48px;
    background: linear-gradient(135deg, #d81324, #b30f1e);
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    box-shadow: 0 6px 18px rgba(216, 19, 36, 0.25);
    transition: transform 0.3s ease;
    position: relative;
  }

  .feature:hover i {
    transform: rotate(360deg);
  }

  .feature-content {
    flex: 1;
    min-width: 0;
    width: 100%;
  }

  .feature h4 {
    color: #1a1a1a;
    font-size: 1.1rem;
    margin: 0;
    font-weight: 700;
    line-height: 1.2;
    white-space: nowrap;
    text-transform: capitalize;
  }

  .feature p {
    color: #555;
    margin: 0;
    font-size: 0.9rem;
    line-height: 1.5;
    font-weight: 500;
    width: 100%;
    word-wrap: break-word;
    hyphens: auto;
  }

  .about-buttons {
    display: flex;
    gap: 20px;
    margin-top: 40px;
  }

  .btn-primary {
    background: #d81324;
    color: white;
    padding: 15px 30px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    transition: all 0.3s;
  }

  .btn-primary:hover {
    background: #b30f1e;
    transform: translateY(-2px);
  }

  .btn-outline {
    background: transparent;
    color: #d81324;
    padding: 15px 30px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    border: 2px solid #d81324;
    transition: all 0.3s;
  }

  .btn-outline:hover {
    background: #d81324;
    color: white;
    transform: translateY(-2px);
  }

  .about-image {
    position: relative;
  }

  .image-container {
    position: relative;
  }

  .image-placeholder {
    width: 100%;
    height: 400px;
    background: linear-gradient(135deg, #d81324, #b30f1e);
    border-radius: 20px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    text-align: center;
    box-shadow: 0 20px 40px rgba(216, 19, 36, 0.3);
  }

  .placeholder-content i {
    font-size: 4rem;
    margin-bottom: 20px;
    display: block;
  }

  .placeholder-content p {
    font-size: 1.3rem;
    font-weight: 600;
    margin: 0 0 10px 0;
    line-height: 1.4;
  }

  .placeholder-content small {
    font-size: 0.9rem;
    opacity: 0.8;
    display: block;
  }

  .image-stats {
    position: absolute;
    bottom: -20px;
    right: -20px;
    display: flex;
    gap: 15px;
  }

  .stat-badge {
    background: white;
    padding: 20px;
    border-radius: 15px;
    text-align: center;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    min-width: 80px;
  }

  .stat-number {
    display: block;
    font-size: 1.8rem;
    font-weight: bold;
    color: #d81324;
  }

  .stat-label {
    display: block;
    font-size: 0.9rem;
    color: #666;
    margin-top: 5px;
  }

  @media (max-width: 768px) {
    .about-section {
      padding: 60px 0;
      overflow-x: hidden;
    }

    .container {
      padding: 0 10px;
      margin: 0 auto;
      width: 100%;
      max-width: 100vw;
      box-sizing: border-box;
    }

    .about-content {
      grid-template-columns: 1fr;
      gap: 30px;
      text-align: center;
      width: 100%;
      max-width: 100%;
      justify-items: center;
      align-items: center;
      box-sizing: border-box;
    }

    .about-text {
      width: 100%;
      max-width: 100%;
      box-sizing: border-box;
      padding: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .about-image {
      width: 100%;
      max-width: 100%;
      box-sizing: border-box;
    }

    .about-text h2 {
      font-size: 2.2rem;
      text-align: center;
      white-space: normal;
    }

    .about-text h3 {
      font-size: 1.2rem;
      margin-bottom: 25px;
      text-align: center;
    }

    .about-text p {
      font-size: 1.05rem;
      text-align: center;
    }

    .about-text p:first-of-type {
      font-size: 1.1rem;
    }

    .features {
      grid-template-columns: repeat(3, 1fr);
      gap: 15px;
    }

    .feature {
      flex-direction: column;
      align-items: center;
      text-align: center;
      padding: 12px 8px;
    }

    .feature i {
      margin-bottom: 8px;
      width: 36px;
      height: 36px;
      font-size: 1rem;
    }

    .feature-content {
      width: 100%;
      text-align: center;
    }

    .feature h4 {
      font-size: 0.9rem;
      margin-bottom: 5px;
      white-space: nowrap;
    }

    .feature p {
      font-size: 0.9rem;
      line-height: 1.6;
    }

    .about-buttons {
      flex-direction: column;
      align-items: center;
    }

    .image-placeholder {
      height: 300px;
    }

    .about-image {
      width: 100%;
      overflow: hidden;
    }

    .image-container {
      position: relative;
      width: 100%;
      overflow: hidden;
    }

    .warehouse-image {
      height: 300px;
      width: 100%;
    }

    .image-stats {
      position: absolute;
      bottom: 10px;
      right: 10px;
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
    }

    .stat-badge {
      padding: 12px;
      min-width: 60px;
    }

    .stat-number {
      font-size: 1.3rem;
    }
  }

  /* Warehouse Image Styles */
  .warehouse-image {
    width: 100%;
    height: 500px;
    object-fit: cover;
    border-radius: 20px;
    transition: transform 0.5s ease;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
  }

  .image-container:hover .warehouse-image {
    transform: scale(1.02);
  }

  .image-overlay {
    position: absolute;
    bottom: 20px;
    left: 20px;
    right: 20px;
    background: linear-gradient(135deg, rgba(0, 0, 0, 0.8), rgba(220, 38, 38, 0.8));
    padding: 1.5rem;
    border-radius: 15px;
    color: var(--white);
    backdrop-filter: blur(10px);
  }

  .overlay-content h4 {
    font-size: 1.25rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
    color: var(--white);
  }

  .overlay-content p {
    font-size: 0.9rem;
    opacity: 0.9;
    margin: 0;
  }

  /* Mission and Vision Styles */
  .mission-vision-section {
    margin-top: 80px;
  }

  .mission-vision-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 40px;
    margin-top: 30px;
  }

  .mission-card,
  .vision-card {
    background: white;
    border-radius: 20px;
    padding: 40px 30px;
    box-shadow: 0 15px 35px rgba(0, 0, 0, 0.08);
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .mission-card::before,
  .vision-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(90deg, #d81324, #e01729);
  }

  .mission-card:hover,
  .vision-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 25px 50px rgba(0, 0, 0, 0.15);
  }

  .card-icon {
    width: 70px;
    height: 70px;
    background: linear-gradient(135deg, #d81324, #b30f1e);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 25px;
    transition: all 0.3s ease;
    box-shadow: 0 8px 25px rgba(216, 19, 36, 0.3);
  }

  .mission-card:hover .card-icon,
  .vision-card:hover .card-icon {
    transform: scale(1.1) rotate(5deg);
    box-shadow: 0 15px 35px rgba(216, 19, 36, 0.4);
  }

  .card-icon i {
    font-size: 1.8rem;
    color: white;
  }

  .card-content h3 {
    font-size: 1.5rem;
    color: #333;
    margin-bottom: 20px;
    font-weight: 700;
  }

  .card-content p {
    color: #666;
    line-height: 1.8;
    font-size: 1.05rem;
    margin: 0;
    text-align: justify;
  }

  @media (max-width: 768px) {
    .mission-vision-section {
      margin-top: 60px;
    }

    .mission-vision-grid {
      grid-template-columns: 1fr;
      gap: 30px;
    }

    .mission-card,
    .vision-card {
      padding: 30px 25px;
    }

    .card-content h3 {
      font-size: 1.3rem;
    }

    .card-content p {
      font-size: 1rem;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const statNumbers = document.querySelectorAll('.stat-number');
    
    const animateCounter = (element, target, suffix, duration = 2000) => {
      const startValue = 0;
      const increment = target / (duration / 16); // 60fps
      let currentValue = startValue;
      
      const timer = setInterval(() => {
        currentValue += increment;
        if (currentValue >= target) {
          currentValue = target;
          clearInterval(timer);
        }
        element.textContent = Math.floor(currentValue) + suffix;
      }, 16);
    };
    
    // Intersection Observer para activar la animación cuando sea visible
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const element = entry.target;
          const target = parseInt(element.dataset.target);
          const suffix = element.dataset.suffix;
          
          // Evitar múltiples animaciones
          if (!element.classList.contains('animated')) {
            element.classList.add('animated');
            setTimeout(() => {
              animateCounter(element, target, suffix);
            }, 500); // Delay para mejor efecto visual
          }
        }
      });
    }, {
      threshold: 0.5 // Activar cuando el 50% del elemento sea visible
    });
    
    // Observar todos los números estadísticos
    statNumbers.forEach(stat => {
      observer.observe(stat);
    });
  });
</script>